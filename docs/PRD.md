# 欠料看板本地 APP — 产品需求文档（PRD）

> 版本：v1.6.6
> 日期：2026-08-21
> 状态：已上线（本地 APP + 云端 Render 双形态运行；**腾讯云轻量应用服务器自托管形态规划中（部分就绪：服务器已开通、系统定型 OpenCloudOS 9.6 + 宝塔、Node.js 20 已装，待部署应用与 CLI 登录）**；功能持续迭代中；新增钣金欠料独立模块；UI 接入壹月科技品牌 VI + 毛玻璃；v1.6.5 修复 Render 跨平台同步崩溃）

---

## 1. 产品目标和使用场景

### 1.1 背景
此前欠料跟踪依赖企业微信里的「机器人」通过聊天方式查询欠料表，缺少标准流程、缺测试、缺存档，且数据未做本地持久化。本次将其**重做成一个本地运行的浏览器 APP**，沿用已验证的业务逻辑（数据来源、解析规则、汇总口径、交期对比、状态过滤），但采用标准开发流程、独立文件夹、本地数据库、可测试。

### 1.2 产品目标
- 提供一个**本地、可离线查询**的欠料看板，不再依赖聊天机器人。
- 数据从企业微信欠料在线表**同步**到**本地数据库**，重启电脑不丢失。
- 覆盖核心查询场景：总览、明细搜索、项目汇总、物料汇总、品牌汇总、交期对比、状态过滤。
- 同步过程对用户可见（进度、上次成功时间、失败提示）。

### 1.3 使用场景
- 采购/跟单人员每日打开 APP，点「同步」拉取最新欠料，查看哪个项目、哪种物料缺多少。
- 按品牌（如富士金、Siemens）汇总，快速判断某品牌的欠料总量与分布。
- 问「某物料交期来不来得及」：用供方「预计交期」对比需求方「期望交期」，给出风险提示。
- 已解决/归档的条目（含隐藏行/分表）自动排除，避免干扰。
- **整表归档（v1.6.4）**：某张分表（子表）末尾单独加一行写「已归档」（无物料编码），则整张分表的所有行都被标记「已归档」、不参与任何统计；与逐行归档走同一套过滤链路。

---

## 2. 页面结构和导航

采用**左侧导航栏 + 顶部工具栏 + 中间/右侧内容区**的经典布局。

### 2.1 顶部工具栏（全局）
- 应用名称 / 当前模块标题
- 「🔄 同步」按钮（立即从企微拉取最新数据）
- 上次成功同步时间 + 各分表条数
- 状态筛选下拉（按「已解决/归档」等关键词，可勾选纳入或排除）

### 2.2 左侧导航（模块）
1. 总览
2. 查询
3. 项目汇总
4. 物料汇总
5. 品牌汇总
6. 交期对比
7. 数据同步
8. 设置
9. 钣金欠料（箱体进度统计，金山文档独立数据源）

### 2.3 视觉识别（VI）· v1.6.1 新增 · v1.6.3 毛玻璃增强
- **品牌**：壹月科技 / JANUARY TECHNOLOGY。
- **品牌色板**（已落实在 `static/style.css :root`）：
  - 主色 青绿 `#23BABB`（按钮、链接、强调条、主指标卡顶条）
  - 主色深 `#0F7B7C` / `#1A9A9B`（hover、深色背景渐变）
  - 主色浅 `#E8F7F8`（卡片轻背景）
  - 副色 黄绿 `#C1D52D`（强调卡片、副标题、装饰条、激活菜单左条）
  - 副色深 `#9FB821`（强调文字）
- **背景层（v1.6.3 新增）**：
  - 浅色品牌渐变 `linear-gradient(150deg, #EAF6F6 0%, #F2F7F8 35%, #EEF5F1 70%, #F6F9EC 100%)`
  - 4 个浮动光斑（`fixed` 定位 + `blur(90px)`）：左上青绿大光斑 / 右下黄绿大光斑 / 中央+右上次级光斑；每个 26-44s 慢速 `alternate` 浮动，作为毛玻璃的视觉底
- **毛玻璃参数（v1.6.3 新增）**：
  - `backdrop-filter: blur(20px) saturate(170%)` + 半透明白背景 `rgba(255,255,255,0.60)` + 细白边 `rgba(255,255,255,0.65)` + 多层阴影
  - 侧栏深色毛玻璃：`linear-gradient(168deg, rgba(10,92,93,0.80), rgba(16,128,129,0.72), rgba(35,186,187,0.62))` + `blur(22px)`
  - 顶栏：`rgba(255,255,255,0.55)` + `blur(20px)` + `position:sticky` 滚动吸附
  - 输入框/选择/徽章：分别用 0.62 / 0.62 / blur(8-12px) 差异化
  - 兜底：`@supports not (backdrop-filter)` 时用 0.92 不透明白背景，保证低版本浏览器可读
- **侧栏**：深色毛玻璃（青绿渐变半透明 + blur 22px）+ 顶部 4px 黄绿装饰条；品牌区用白底卡片展示彩色 LOGO；底部状态指示灯带脉动动画。
- **内容区**：浅色渐变 + 浮动光斑 + 毛玻璃面板/卡片；面板标题前 3px 主色小色条（带 glow 阴影）；指标卡顶部 3px 主色 / 副色装饰条 + 顶部白色高光层，悬停上浮 -3px 强化。
- **按钮**（v1.6.3 升级）：胶囊形 `border-radius: 20px` + 品牌渐变 + `box-shadow: 0 2px 8px rgba(35,186,187,0.35)` + hover 上浮 -1px + 阴影加深 + active 缩 0.97。
- **切换动画**（v1.6.3 新增）：
  - `pageIn` 关键帧：opacity 0→1 + translateY 16→0 + scale 0.985→1 + filter blur(2)→0 同步缓动
  - 0.34s `cubic-bezier(0.22, 1, 0.36, 1)`（Material iOS 风格）
  - 抽 `showPage(page, title)` 统一导航/跳转项目/初始化三处切换；强制 `offsetWidth` reflow 保证每次重新触发
- **其他动效**：告警条 slide-down 入场；侧栏菜单 hover 右移 3px + active 渐变高亮 + translateX(2px)；卡片 hover translateY(-3px) + 阴影升级 + 白边亮起。
- **状态色**：danger `#DC2626` / success `#16A34A` / warning `#F59E0B`，对应浅色背景。
- **滚动条**：青绿主题，宽度 8px，thumb 半透明。
- **响应式**：屏幕 < 720px 时侧栏自动收起为 64px 图标条。
- **Logo 资源**：`static/logo.png`（主显示）、`static/favicon.png`（浏览器标签图标）。
- **未来扩展**：底色、字体、毛玻璃参数、动效统一收敛在 `:root` CSS 变量里；后续新增模块、页面只需复用相同 token 即可保持 VI 一致性。

### 2.4 内容区（随导航切换，见第 3 节）

---

## 3. 各模块的功能和用途

### 3.1 总览（Dashboard）
- **用途**：一眼掌握全局欠料态势。
- **功能**：
  - 5 张指标卡：总欠料条数、合计欠料数量、紧急条目数（空白/无交期/付款瓶颈）、已同步分表数、**本周新增的材料采购及时率**。
  - **本周新增采购及时率口径**：
    - 通过同步快照对比识别“真正新增”：每次同步保存全部 `项目编码|物料编码` 标识集合，下次同步时找出新增的组合，写入 `weekly_new_items`。
    - “及时”判定：新增条目的状态字段包含“已到货”或“已解决/已完成”。
    - 显示：及时率百分比 + 及时数/总数；本周无新增时显示“本周暂无新增”。
  - 紧急度分布图（空白 / 无交期 / 付款瓶颈 / 有交期 / 其他）。
  - 项目欠料 TOP10 条形图。
  - 点击项目条 → 跳「项目汇总」带入该项目。

### 3.2 查询（Search）
- **用途**：按关键词检索欠料明细。
- **功能**：
  - 输入框 + 类型下拉（自动 / 项目 / 物料编码 / 物料名称 / 品牌）+ 搜索按钮。
  - 结果表格字段：项目、物料编码、物料名称、品牌、规格、欠料数量、紧急度、预计到货、状态。
  - 支持口语：自动剥离「XX项目」「XX品牌」「XX材料」等尾缀（如「富士金品牌」「巨茂项目」）。
  - 表头排序、点击行展开详情。

### 3.3 项目汇总（Project Summary）
- **用途**：查看某项目整体欠料与按物料的明细；未输入时默认展示全部项目汇总。
- **功能**：
  - 项目名/编码输入；**留空则展示全部项目列表**（项目、合计数量、紧急度、来源表），按欠料量排序。
  - 输入后输出：总条数、合计欠料量、紧急度分布、**以物料编码为单位**的缺料明细（编码、名称、品牌、合计数量、紧急度）。
  - 点击左侧「项目汇总」导航即自动加载默认全部项目；搜索后切换为该项目明细。

### 3.4 物料汇总（Material Summary）
- **用途**：跨项目看某物料的欠料总量；未输入时默认展示全部物料汇总。
- **功能**：
  - 物料编码/名称输入（如 `B07-05-00-03-10`、`减压阀`、`Siemens`）；**留空则展示全部物料列表**（编码、名称、合计数量、涉及项目数、紧急度）。
  - 输入后额外展示：按项目分布、明细 Top20。
  - 点击左侧「物料汇总」导航即自动加载默认全部物料；搜索后切换为过滤结果。

### 3.5 品牌汇总（Brand Summary）★ 新增
- **用途**：按品牌维度汇总欠料；未输入时默认展示全部品牌汇总。
- **功能**：
  - 品牌输入或下拉（如 富士金 / Siemens）；**留空则展示全部品牌列表**（品牌、合计数量、涉及物料数、紧急度）。
  - 输入后输出：该品牌总条数、合计欠料量、按物料编码分布、按项目分布、明细表。
  - **按物料编码分布表字段**：物料编码、名称、**型号（规格说明）**、合计数量、项目数、紧急度、来源表。
  - 点击左侧「品牌汇总」导航即自动加载默认全部品牌；搜索后切换为该品牌明细。

### 3.6 交期对比（ETA Check）
- **用途**：判断某条目/项目交期是否赶得上需求；未输入时默认展示全部欠料的交期对比。
- **功能**：
  - 项目/物料输入；**留空则展示全部欠料**的交期对比卡片。
  - **按物料编码聚合**：同一物料编码跨项目/多行合并为一张卡片，顶部显示编码、名称、涉及项目。
  - 每张卡片内按项目列出「需求交期（期望交期） vs 实际预计到货（预计到货时间）」的时间轴对比：
    - 灰色竖线 = 今天；橙色标记 = 需求交期；蓝色标记 = 实际预计到货；
    - 两标记之间红色段 = 逾期缺口，绿色段 = 提前余量；
    - 文字标注剩余/逾期天数、相差天数、判定结果（逾期 / 提前 / 刚好 / 缺交期信息）。
  - 顶部指标卡：匹配条数、逾期风险、来得及、缺交期信息。
  - ⚠️ 注意：`预计到货时间` 只读供方承诺列（预计到货时间 / 材料预计到货时间），**不读**需求方写的「期望到货时间」；期望交期仅用于对比。

### 3.7 数据同步（Sync）
- **用途**：从企微欠料表拉取最新数据入库。
- **功能**：
  - 「立即同步」按钮 + 进度条 + 日志区（成功/失败条数、耗时）。
  - 显示上次成功时间、各分表条数、失败提示与重试。
  - 失败时保留上次成功数据，不覆盖。

### 3.8 设置（Settings）
- **用途**：配置与查看。
- **功能**：
  - 数据源标识（企微表，只读展示，复制自原项目）。
  - 本地数据库路径。
  - 状态过滤关键词管理（已解决/归档/已完成/关闭/已到货/取消/作废 可勾选）。

### 3.9 钣金欠料（SheetMetal）★ 新增（v1.5）

- **用途**：把金山文档「箱体进度统计」表的箱体欠料单独建一个**独立模块**，与企微主线（§3.1–§3.8）平行，数据源、数据库、同步、导航页均独立，不混用。
- **数据源**：金山文档「箱体进度统计」（file_id `HeqbtFcx3rMqYYFRpA9n1xZrPzbTeaL4X`，URL `https://www.kdocs.cn/l/csUkRQG8wIXk`）。仅以下两个**明细分表**计入欠料（其余为汇总表，不计）：
  - `5A-巨茂箱体进度8.17`（巨茂，13 列：序号/图纸批次/图纸时间/项目名称/供应商/設備類別/品名/规格型号图纸编号/数量/到货情况/预计到货时间/实际到货时间/壹月发货批次）
  - `箱体进度统计（非巨茂）`（非巨茂，12 列：序号/图纸时间/项目名称/供应商/采购订单号/物料编码/品名/规格型号图纸编号/数量/到货情况/预计到货时间/实际到货时间）
- **同步**：本地 `kdocs-cli`（`C:/Users/Apua/.local/bin/kdocs-cli.exe`，需先 `kdocs-cli auth login`）拉取两个分表 → 归一化 → 写入独立库；或用 `_bootstrap_sheetmetal.py` 一键初始化。失败保留旧数据。
- **存储**：独立 SQLite `data/sheetmetal.db` + 快照 `data/seed_sheetmetal.sql`（随 push 触发 Render 重部署，机制同 §9.2）。
- **表字段**（`sheetmetal_items`）：分表 `sheet`、发货批次 `batch`（仅巨茂有，取「壹月发货批次」表头）、图纸批次 `drawing_batch`、图纸时间 `drawing_date`、交货时间 `delivery_date`、项目 `project`（按项目名称区分，巨茂只是其中一个项目）、设备类别 `category`、供应商 `supplier`、采购单 `po_no`、物料编码 `material_code`、名称 `name`、规格 `spec`、数量 `qty`、到货情况 `arrival`、预计 `eta`、实际到货 `arrival_date`、备注 `note`、`synced_at`。
- **到货判定**：`arrival` 含「已到货」视为已到货；其余视为欠料（无独立"状态列"过滤，以到货情况为准）。
- **功能（前端页 `page-sheetmetal`）**：
  - 指标卡：总量 / 总数量 / 已到货 / 欠料条数 / 欠料数量（已移除"分表数"卡片）。
  - 分布面板：**项目分布**（按项目名称区分，巨茂为其中之一；条形拆分为已到货/未到货两段，各项目未到货条目按「预计到货(ETA)」显示剩余/逾期天数，全部到货则不显示）、设备类别分布、供应商分布（整表聚合，跨所有项目）、发货批次分布（仅巨茂，按「壹月发货批次」表头；条形拆分为已到货/未到货两段，未到货批次按「批次交期(批次名)」显示剩余/逾期天数）。不再单独统计分表分布。
  - 查询：跨全部字段模糊搜索（含发货批次/图纸批次）+ 筛选（全部 / 仅欠料 / 仅已到货），结果表格含「已到货」标记。
  - 同步：「🔄 同步钣金」按钮 + 同步状态提示。
- **API**：`/api/sheetmetal_overview`、`/api/sheetmetal_search`、`/api/sheetmetal_sync_status`、`/api/sheetmetal_settings`。
- **已知限制**：到货判定仅看「到货情况」文本，未做结构化状态列；业务上两个分表仍以「项目名称」为统一区分维度，巨茂只是其中一个项目（未来可合并为单一分表）。

---

## 4. 模块之间的关系

```
数据同步 ──写入──> 本地数据库 <──读取── 所有查询/汇总模块
                                      │
总览 = 数据库全量聚合（指标卡 + 紧急度 + 项目TOP）
查询 = 数据库模糊检索（项目/编码/名称/品牌）
项目汇总 = 查询(项目) + 按物料编码聚合
物料汇总 = 查询(物料) + 按物料编码跨项目聚合
品牌汇总 = 查询(品牌) + 按物料编码/项目聚合
交期对比 = 查询 + 预计 vs 期望 日期对比
所有模块 ──统一经过──> 状态过滤（排除已解决/归档）
```

- 数据同步是**唯一写入**入口；其余模块均为**只读**查询。
- 状态过滤是所有查询的统一前置（在查询入口排除已解决/归档条目）。

---

## 5. 本地数据保存和备份要求

- **存储**：本地 SQLite 数据库文件（位于 APP 文件夹内，如 `data/shortage.db`）。
- **持久化**：所有同步结果入库；APP 关闭、电脑重启后从库读取，**无需重新同步即可查询**。
- **同步原子性**：同步成功才提交；失败保留上次成功数据。
- **备份**：
  - 数据库文件随项目文件夹，纳入 Git 版本管理（见 Step 6）。
  - 建议定期复制 `data/shortage.db` 作离线备份（第一版可手动，后续可加自动备份）。
- **隔离**：新项目位于独立文件夹 `D:\InvoiceTool\shortage_app\`，**不修改、不移动、不删除**原 `shortage_tool` 任何文件。

---

## 6. 第一版暂不做的功能

- ❌ 写回到货时间到企微表（后续二次开发）。
- ❌ 聊天式自然语言输入、企微群机器人交互（改为 APP 内结构化操作）。
- ❌ 责任到人/按采购人推送（原计划的第二阶段，依赖品牌分工表，v1 不含）。
- ❌ 界面美化（Step 9，功能优先）。
- ✅ 服务端部署：已完成。v1.1 起同时提供「本地 APP」与「云端 Render（https://shortage-app.onrender.com/）」双形态；云端数据来自本地自动同步推送的 seed.sql 快照（见第 9 节）。

---

## 7. 验证标准

1. **数据同步**：点「同步」能从企微拉到数据并入库；显示条数与上次同步时间；网络失败时不崩、保留旧数据。
2. **持久化**：同步后关闭并重启 APP，数据仍在（不重新同步也能查）。
3. **总览**：指标卡与分布图数据正确，与数据库一致。
4. **查询**：
   - `富士金品牌` 能命中品牌「富士金」的全部条目（自动剥离尾缀）。
   - `巨茂项目` 能模糊匹配到「合肥巨茂材料科技有限公司-…」项目。
   - `B07-05-00-03-10` 能搜到该编码明细。
5. **项目汇总**：`巨茂项目` 汇总按物料编码给出明细与合计，数量与明细一致。
6. **物料汇总**：`B07-05-00-03-10` 给出跨项目合计总数。
7. **品牌汇总（新增）**：`富士金` 给出该品牌合计欠料量、按物料编码分布、按项目分布。
8. **交期对比**：带期望交期的条目能正确判定 ✅/⚠️ 及相差天数；`预计到货时间` 不误读「期望到货时间」。
9. **状态过滤**：标记「已解决/归档」的条目（含隐藏行/分表）在以上所有查询中自动排除。
10. **测试**：解析、各汇总、状态过滤、API 冒烟均有单元测试，全部通过。

---

## 8. 关键业务规则（从原项目继承，须严格遵守）

- **交期语义**：`预计到货时间` 仅取供方承诺列；`期望到货时间` 是需求方写的，单独存为「期望交期」，仅用于交期对比。
- **列名别名**（不同子表表头不一致，解析时需自适应）：
  - 项目：项目 / 项目名称 / 申请部门 / 领用车间（新增「项目名称」以适配规范后的在线表表头）
  - 欠料数量：欠料数量 / 待领数量
  - 预计交期：预计到货时间 / 材料预计到货时间
  - 期望交期：期望到货时间 / 期望交期
  - 状态：状态 / 处理状态 / 进度 / 是否解决 / 备注
- **状态过滤关键词**：已解决/归档/已完成/完成/已关闭/关闭/已领/已到货/已到/解决/取消/作废（命中即排除）。
- **口语尾缀剥离**：查询关键词提取后须剥离「品牌/材料/物料/型号/系列/规格/配件/零件/项目/公司/厂家」等尾缀。
- **隐藏行/分表**：markdown 导出不含隐藏标记，无法自动识别；可靠机制是状态列过滤。用户后续会在表里加状态列；加列前隐藏行仍计入（已知限制）。
- **纵向合并单元格**：wecom-cli 导出 markdown 时合并单元格只在首行保留值。解析时对 项目编码/项目/单据编号/申请部门/单据日期/期望交期/预计到货时间 做前向填充；其中日期类字段（期望交期/预计到货时间）限制在同一单据编号内填充，避免跨单据污染。
- **人工到货/解决覆盖**：用户在项目汇总界面点击「确认到货」或输入快速登记文本后，系统把（项目 + 物料编码）写入 `manual_overrides` 表，并在同步后自动将该组合标记为 `manual_status = 已到货（人工）`，从而在看板中剔除；该记录随 `seed.sql` 同步到云端。
- **写回到货（方案 B，v1.3 新增）**：确认到货时，除本地覆盖外，还会**回写企业微信在线表对应单元格的状态列**（默认写「已到货」）。写前经用户预览确认（展示将改哪个子表、第几行、旧值→新值），并带多重安全闸（子表单表连续、唯一命中、坐标在界内、状态列存在）。**前提**：连接的智能机器人需对该在线表拥有【编辑】权限，否则写回会返回 `851003 no authority`（本地看板仍正常更新）。详见第 10 节。

---

## 9. 部署与运行架构（v1.1 新增，v1.2 补充人工覆盖）

### 9.1 三种运行形态（含规划中自托管）
- **本地 APP**：Windows 上 `pythonw` 后台常驻（注册表 `HKCU\Run\ShortageApp` 登录自启，无窗、不弹黑窗），访问 `http://localhost:8765`。
- **云端部署**：Render Web Service（`https://shortage-app.onrender.com/`），Auto-Deploy 开启；代码 push 或数据 `seed.sql` push 均触发重部署。
- **自托管（规划中，部分就绪）**：腾讯云轻量应用服务器 Lighthouse（实例 `lhins-pgehtw8y`，ap-beijing，OpenCloudOS 9.6 + 宝塔面板，2C2G/50G SSD/4Mbps）。已装 Node.js 20 LTS。目标：服务器自身成为同步源（见 §9.2），云端真正在线同步，不再依赖本地电脑常开与 `seed.sql` 推送 hack。**当前尚未部署应用代码、未安装/登录 wecom-cli 与 kdocs-cli、未配 systemd 与 nginx**，`wecom-cli init` / `kdocs-cli auth login` 为交互式（扫码/浏览器），须用户在服务器终端完成。

### 9.2 数据同步与一致性
- 本地为**唯一写入源**：`app.py` 后台定时从企微同步 → 写入 SQLite → 导出 `seed.sql` 快照 → 推送 GitHub。
- 云端无 wecom-cli / 企微登录态，启动时从 `seed.sql` 初始化（含 `meta.last_sync` / `last_count`，故云端能显示「上次同步时间」）。
- 单实例保护：Windows 命名互斥体 `Global\ShortageAppSingleton`，防重复实例互相干扰。
- 可插拔数据源：配置 `WECOM_API_CORP_ID/SECRET/TABLE_DOCID` 时云端可直连企微 API；未配置则回退 wecom-cli（本地）。
- **失效代理自动清理（v1.6.5）**：调用 wecom-cli / kdocs-cli / git 前，由 `sync._clean_env_for_subprocess()` 检测并剔除指向不可用端口的 `HTTP_PROXY/HTTPS_PROXY/ALL_PROXY`，避免旧代理软件遗留环境变量导致外部命令连不上企微/GitHub。
- **跨平台兼容（v1.6.5）**：所有 `subprocess.run` 调用仅在 Windows 上传 `CREATE_NO_WINDOW`；Linux/macOS（Render）不再因 `creationflags` 参数崩溃。云端未配置 API 且未检测到 wecom-cli 时，`sync.can_sync_online()` 返回 `False`，前端显示「快照模式」提示，禁止无效重试。
- **自托管形态下的同步源变更（规划中）**：当部署到腾讯云 Lighthouse 且两套 CLI 已安装并登录后，**服务器自身即成为唯一写入源**——`app.py` 直接在服务器上每 30 秒同步写本地 SQLite，前端「同步」按钮真正可用；此时不再需要「本地 push `seed.sql` → 触发 Render 重部署」的快照链路，应关闭 seed.sql 自动推送（`app.py` 的 `_export_and_check_seed` 调用），改为纯服务端运行。路径配置：Linux 上 `KDOCS_CLI` 须用环境变量指向 `/usr/bin/kdocs-cli`（代码默认 Windows 路径在 Linux 不存在），wecom-cli 走 `sync._detect_wecom_cli()` 的 Linux 候选路径；进程守护改用 systemd / 宝塔 supervisor 替代 pythonw + 看门狗。

### 9.3 已知限制
- 云端数据实时性依赖本地进程常开 + 自动推送；本地关机期间云端数据为最近一次快照。
- 在线表表头需保持约定列名（含「项目」或「项目名称」），否则物料会 fallback 到申请部门/领用车间；已对双「项目编码」异常表头做兜底（详见 CHANGELOG `33ae048` / `983ed8a`）。

---

## 10. 人工到货/解决覆盖与在线表写回 —— 已删除（v1.4）

### 10.1 原功能
- **本地人工覆盖**：项目汇总每行提供「确认到货」按钮，顶部提供「快速到货登记」文本框；命中行写入 `manual_overrides` 表，并在同步后通过 `manual_status` 列在看板中隐藏。
- **在线表写回（方案 B）**：本地标记时同步把企业微信在线表对应行的状态列改写为「已到货」，使源头数据一致。

### 10.2 删除原因（2026-08-13）
- 企业微信 **10 人以上企业模式**下，API 模式机器人仅可编辑**自己创建**的文档/表格，不会继承授权用户对成员手工创建文档的编辑权（官方帮助文档明文限制）。
- 欠料在线表由成员手工创建，机器人调 `sheet_update_range_data` 始终返回 **`851003 no authority`**，即便已在后台授权「新建和编辑文档」仍无效——这是平台对象级权限限制，非代码缺陷、非文档分享权限问题。
- 在无法写回原始表的情况下，单独的「本地标记/隐藏」功能对用户无意义；经用户确认，**彻底删除**「确认到货」「快速到货登记」、`manual_overrides` 表及 `manual_status` 列。

### 10.3 当前行为
- 项目汇总仅保留只读欠料明细，无操作列、无到货登记入口。
- 所有欠料/到货状态以企业微信在线表的「状态」列为准；若需隐藏某行，请直接修改原始在线表的状态列（如改为「已到货」「已归档」「已解决」），下次同步后看板自动过滤。
- `writeback.py` 已从仓库移除；`sync.py` 的 `sheet_get_info` / `sheet_update_range_data` 等写接口封装亦被移除。如未来需要写回能力，须改用企业微信「自建应用」官方 API 重新实现。
